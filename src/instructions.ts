// Auto-generated. High-level routing hint surfaced to MCP host LLMs via
// the McpServer `instructions` field.
export const SERVER_INSTRUCTIONS = "Nuntly is a transactional email platform. Use these tools to send transactional and bulk\nemail, manage sending domains and webhooks, and work with inbound mail (namespaces, inboxes,\nthreads, messages).\n\nGuidance:\n- send-email sends a single message; send-bulk-emails sends many in one request.\n- List operations are paginated with cursor + limit (limit max 30); pass the returned\n  nextCursor to fetch the next page.\n- delete-* operations are destructive: confirm with the user before calling them.\n- IDs are prefix-coded: em_ (email), dns_ (domain), wh_ (webhook), ns_ (namespace),\n  ibx_ (inbox), thr_ (thread), imsg_ (inbound message).\n- On failure a tool returns the HTTP status, message, and request id; surface the request\n  id to the user when reporting an error.";
